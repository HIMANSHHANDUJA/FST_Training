-----ACTIVITY 8
select * from orders;
SELECT customer_id, MAX(purchase_amount) AS "Max Amount" FROM orders GROUP BY customer_id;
SELECT customer_id, MAX(purchase_amount) AS "Max Amount" where ORDER_DATE='2012-08-17' GROUP BY customer_id;
SELECT customer_id, MAX(purchase_amount) AS "Max Amount" , ORDER_DATE from orders
GROUP BY customer_id, order_date
HAVING MAX(purchase_amount) IN(2030, 3450, 5760, 6000);
